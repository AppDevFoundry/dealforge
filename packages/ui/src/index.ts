/**
 * @dealforge/ui
 *
 * Shared React components for the DealForge platform.
 *
 * Components are organized by category:
 * - components/layout: Layout primitives (Container, Stack, etc.)
 * - components/forms: Form controls (Input, Select, etc.)
 * - components/charts: Data visualization
 * - components/calculators: Calculator-specific components
 *
 * Most UI components will live in apps/web using shadcn/ui.
 * This package is for truly shared components used across multiple apps.
 */

// Export shared components here as they are created
// export { Button } from './components/button';
