[package]
name = "calc-engine"
version = "0.1.0"
edition = "2021"
authors = ["DealForge Contributors"]
description = "Real estate investment calculation engine"
license = "MIT"
repository = "https://github.com/yourusername/dealforge"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
approx = "0.5"
